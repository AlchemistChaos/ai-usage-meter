name: Release

# Tag a version to cut a release:  git tag v0.1.0 && git push --tags
on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Resolve version
        id: v
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # Signing and notarization are optional: without the secrets the job
      # still produces an ad-hoc signed DMG, it just isn't Gatekeeper-clean.
      - name: Check signing secrets
        id: secrets
        run: |
          if [[ -n "${{ secrets.MACOS_CERTIFICATE }}" ]]; then
            echo "signing=true" >> "$GITHUB_OUTPUT"
          else
            echo "signing=false" >> "$GITHUB_OUTPUT"
            echo "::warning::No signing secrets — producing an unsigned build."
          fi

      - name: Import Developer ID certificate
        if: steps.secrets.outputs.signing == 'true'
        env:
          CERT: ${{ secrets.MACOS_CERTIFICATE }}
          CERT_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ github.run_id }}
        run: |
          echo "$CERT" | base64 --decode > /tmp/cert.p12
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -lut 21600 build.keychain
          security import /tmp/cert.p12 -k build.keychain -P "$CERT_PASSWORD" \
            -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PASSWORD" build.keychain
          rm /tmp/cert.p12
          echo "CCM_SIGN_IDENTITY=$(security find-identity -v -p codesigning \
            | grep 'Developer ID Application' | head -1 \
            | sed -E 's/.*"(.*)"/\1/')" >> "$GITHUB_ENV"

      - name: Build app
        run: ./build-app.sh release

      - name: Package DMG
        run: |
          mkdir -p dist/stage
          cp -R CCManager.app dist/stage/
          ln -s /Applications dist/stage/Applications
          hdiutil create -volname "Notch Limits" -srcfolder dist/stage \
            -ov -format UDZO "dist/NotchLimits-${{ steps.v.outputs.version }}.dmg"

      - name: Notarize and staple
        if: steps.secrets.outputs.signing == 'true'
        env:
          KEY: ${{ secrets.APPLE_API_KEY }}
          KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          ISSUER_ID: ${{ secrets.APPLE_API_ISSUER_ID }}
        run: |
          DMG="dist/NotchLimits-${{ steps.v.outputs.version }}.dmg"
          # Sign the DMG itself so the staple sticks to the download.
          codesign --force --sign "$CCM_SIGN_IDENTITY" --timestamp "$DMG"
          echo "$KEY" | base64 --decode > /tmp/api_key.p8
          xcrun notarytool submit "$DMG" \
            --key /tmp/api_key.p8 --key-id "$KEY_ID" --issuer "$ISSUER_ID" \
            --wait --timeout 30m
          xcrun stapler staple "$DMG"
          rm /tmp/api_key.p8
          spctl -a -vv -t install "$DMG" || true

      - name: Checksum
        id: sum
        run: |
          DMG="dist/NotchLimits-${{ steps.v.outputs.version }}.dmg"
          echo "sha256=$(shasum -a 256 "$DMG" | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/*.dmg
          generate_release_notes: true
          body: |
            ### Install

            ```bash
            brew install --cask everyai-com/tap/notch-limits
            ```

            Or download the DMG below and drag to Applications.

            `sha256: ${{ steps.sum.outputs.sha256 }}`
